import ReactLoading from 'react-loading';
import React from 'react'

const ReactLoader = () => {
    return <ReactLoading type={'spin'} color={'#f4f5f7'} height={'20px'} width={'20px'} />
}

export default ReactLoader